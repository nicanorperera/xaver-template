<%= simple_form_for Usuario.new, :url => sesiones_path do |f| %>
  <%= f.input :email, :autofocus => true %>
  <%= f.input :password %>
  <%= f.button :submit, "Iniciar sesión", :data => {:disable_with => t("espere")}, :class => 'btn btn-large btn-primary' %>
<% end %>