<%= simple_form_for foto, :url => url, :html => { :class => :normal } do |f| %>
  <%= f.input :titulo, :autofocus => true %>
  <%= f.button :submit, :name => nil, :disable_with => t("espere") %>
<%- end -%>